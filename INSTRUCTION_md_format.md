# Prompt：将材料转换为 PPTX 用 Markdown 格式（完整 Markdown 支持版）

请将我提供的材料整理成适合生成 PowerPoint 演示文稿的 Markdown 文件。转换器支持常用 Markdown 语法，但请仍以“幻灯片可读性”和“生成稳定”为优先。

---

## 一、幻灯片基本结构

### 1. 幻灯片分隔符：`---`

建议每张幻灯片之间使用单独一行 `---` 分隔。

```markdown
## 第一页标题

- 第一页内容。

---

## 第二页标题

- 第二页内容。
```

- `---` 单独占一行时表示新幻灯片。
- 如果未使用 `---`，转换器也会尽量按 `##` 标题拆分幻灯片。
- 为保证稳定，推荐显式使用 `---`。

### 2. 封面页：`#`（可选）

如需封面页，在文件**最开头**用 `#` 写标题（只能出现一次）：

```markdown
# 演示文稿主标题
演讲人姓名 | 年份
```

- `#` 标题渲染为封面页的大白字主标题（使用模板封面的背景图和色块）
- 紧跟在 `#` 行之后（中间无空行），可写一行 `演讲人 | 年份` 格式的副行
- 副行中的 `|` 两侧内容分别渲染（模板中为青色），`|` 本身为白色
- 副行建议控制在 **20 个字符以内**（含 `|` 两侧空格），超出可能折行
- 如不需要封面，直接从 `##` 开始即可

### 3. 内容页标题：`##`

每张内容幻灯片以 `##` 标题开始。

```markdown
## 幻灯片标题
```

- 标题应简洁，建议控制在 30 个字以内。
- `## Slide 1｜标题`、`## Slide 1 - 标题` 等写法会自动提取后半部分作为标题。

### 4. 页内小标题：`###` 至 `######`

页内分组标题可使用 `###`，也支持更深层级标题。

```markdown
### 小节标题
```

- 建议优先使用 `###`。
- 小节标题建议控制在 20 个字以内。
- 更深层级标题会降级为页内小标题显示。

---

## 二、正文内容语法

### 1. 普通段落

可以直接写普通文本段落。

```markdown
本页用于概括项目目标和整体策略，重点说明本项目并非单一诉讼。
```

- 普通段落适合用于封面说明、背景说明或过渡说明。
- 不宜在一页放置过长段落，建议每段控制在 80 字以内。

### 2. 无序列表

支持 `-`、`*`、`+` 三种无序列表。

```markdown
- 第一项内容。
* 第二项内容。
+ 第三项内容。
```

- 推荐统一使用 `-`。
- 每条要点建议控制在 60 字以内。
- 支持基本缩进层级，但为保证 PPT 可读性，不建议超过两级。

### 3. 有序列表

支持 `1.`、`2.` 或 `1)`、`2)` 等有序列表。

```markdown
1. 完成诉前评估。
2. 固定被诉产品样品。
3. 启动诉讼和行政程序。
```

- 有序列表适合步骤、阶段、流程。
- 转换器会保留原始编号文本。

### 4. 引用块

支持 `>` 引用块，渲染为左侧灰色竖线 + 缩进文字的强调样式。

```markdown
> 专利诉讼中，买到产品并不难；难的是让法院相信送检产品的同一性。
```

- 适合放置结论句、警示语或需要视觉强调的关键判断。
- 每条 `>` 行是一个独立引用块；连续多行 `>` 会各自独立渲染为独立竖线块。
- 建议每页不超过 **2 个**引用块，否则视觉过于零散。
- 引用块占用的纵向空间约等于 1–2 条要点，规划容量时需计入。

### 5. 代码块

支持 fenced code block：

````markdown
```python
print("hello")
```
````

- 代码块会以等宽字体显示。
- 代码内容过长时可能被压缩或截断。
- 法律或商业 PPT 中一般不建议大量使用代码块。

### 6. 行内代码

支持行内代码：

```markdown
请注意 `##` 表示幻灯片标题。
```

- 行内代码适合标识命令、文件名、参数或 Markdown 标记。

---

## 三、表格

支持标准 GitHub Flavored Markdown 管道表格。

```markdown
| 风险类型 | 具体风险 | 应对措施 |
| --- | --- | --- |
| 方法专利举证风险 | 难以直接适用举证责任倒置 | 推动法院要求被告说明制造方法 |
| 参数鉴定风险 | 单方鉴定可能被质疑 | 强化检材来源和检测方法 |
```

- 表格必须包含表头行、分隔行和至少一行数据。
- 分隔行必须使用 `| --- | --- |` 或同类标准写法。
- 建议每张表不超过 5 列、8 行。
- 单元格尽量控制在 40 字以内。
- 复杂表格应拆成多页，而不是压缩到一页。
- 表格页通常不宜再放置大量项目符号。

---

## 四、图片

支持本地图片语法。

```markdown
![图片说明](images/example.png)
```

- 支持相对于 Markdown 文件所在目录的本地图片路径。
- 如图片不存在或为远程 URL，转换器会显示图片占位提示。
- 建议每页最多 1–2 张图片。
- 图片说明会作为占位或辅助文本使用。

---

## 五、行内格式

### 1. 粗体

```markdown
这是 **重点内容**。
```

- 粗体会尽量转换为 PPT 中的加粗文字。
- 可用于强调关键词，但不要整段加粗。

### 2. 斜体

```markdown
这是 *强调说明*。
```

- 斜体会尽量转换为 PPT 中的斜体文字。
- 中文内容中不建议大量使用斜体。

### 3. 链接

```markdown
请参见 [CNIPA 官网](https://www.cnipa.gov.cn)。
```

- 转换器会显示链接文本。
- URL 通常不会作为可点击链接保留。
- 如 URL 本身很重要，请直接写入正文。

### 4. 图片行内语法

```markdown
![示意图](figure.png)
```

- 单独一行图片语法会作为图片处理。
- 嵌入在段落中的图片语法会降级显示为图片说明文本。

---

## 六、Speaker Notes

支持以下写法添加讲者备注：

```markdown
> **Speaker note:** 这里写演讲者自己看的提示。
> 可继续写第二行备注。
```

- 该内容**不会**显示在幻灯片正文上。
- 转换器会将其写入 PowerPoint 的**笔记栏（Notes Pane）**，在演讲者视图中可见。
- 每页最多一个 Speaker Note 块，放在页面内容末尾、`---` 之前。

---

## 七、每页容量建议

模板内容区域有限，请控制每页信息密度：

| 项目 | 建议值 | 上限 |
| --- | --- | --- |
| 每页小标题数 | 2–3 个 | 4 个 |
| 每页要点总数 | 5–8 条 | 10 条 |
| 每条要点字数 | 30–50 字 | 60 字 |
| 表格列数 | 2–4 列 | 5 列 |
| 表格行数 | 3–6 行 | 8 行 |

- 内容太多时，应拆分为多张幻灯片。
- 宁可多一页，也不要把一页塞满。
- 表格页应减少正文要点。
- 代码页和图片页也应减少其他文字。

---

## 八、不建议使用的内容

以下内容虽然可能被降级处理，但不建议用于正式 PPT 生成：

- HTML 标签或复杂内嵌 HTML。
- 复杂嵌套列表超过两级。
- 一个幻灯片内多个大型表格。
- 超长代码块。
- 远程图片 URL。
- 脚注、数学公式、任务列表、Mermaid 图等复杂扩展语法。

---

## 九、完整示例

```markdown
# 专利维权项目工作方案
张律师 | 2025

---

## 项目目标与整体策略

本项目并非单一诉讼，而是一组围绕产品 X 展开的系列维权行动。

### 三条主线

- 第一，围绕三件发明专利分别设计侵权比对和证据路径。
- 第二，提前收集销售额、利润率和销售持续时间等赔偿证据。
- 第三，通过保全、鉴定和行政程序提高执行压力。

> **Speaker note:** 本页重点说明项目不是单案诉讼，而是整体证据工程。

---

## 项目风险矩阵

| 风险类型 | 具体风险 | 应对措施 |
| --- | --- | --- |
| 方法专利举证风险 | 难以直接适用举证责任倒置 | 通过初步举证推动被告说明制造方法 |
| 参数鉴定风险 | 单方鉴定可能被质疑 | 强化检材来源、封存过程和检测方法 |
| 赔偿证据风险 | 销售额和利润率证据不足 | 诉前收集销售规模和行业利润率证据 |
```

---

## 十、输出要求

- 只输出 Markdown 文本，不要附加解释、说明或前言。
- 不要输出文件名或任何非 Markdown 内容。
- 第一张幻灯片直接从标题开始。
- 建议使用 `---` 明确分隔每张幻灯片。

---

现在请处理我提供的以下材料：

[在此粘贴你的材料]
