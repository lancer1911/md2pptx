# Prompt：将材料转换为 MD2PPTX Markdown 格式（常用 Markdown + SmartArt 支持版）

请将我提供的材料整理成适合本项目 `md_to_pptx.py` 生成 PowerPoint 演示文稿的 Markdown 文件。目标不是展示 Markdown 技巧，而是生成**结构清楚、信息完整、版面稳定、便于客户阅读**的 PPTX。

---

## 1. 任务目标

请在不丢失实质信息的前提下，将材料改写为适合幻灯片展示的 Markdown：

- 保留材料中的法律结论、技术特征、数字、时间、金额、案号、专利号、主体名称、文件名称和关键限定。
- 可根据逻辑重新分页、增加小标题、拆分长段落，但不得删减关键内容。
- 如果一页内容过多，应拆成多页，而不是压缩字号或删除信息。
- 输出应当能由 `md_to_pptx.py` 稳定转换为 PPTX。

---

## 2. 当前转换器支持的内容

本地转换器目前支持：

- 封面页：`#`
- 内容页：`##`
- 页内小标题：`###` 至 `######`
- 普通段落
- 无序列表和编号列表
- 行内加粗、斜体、加粗斜体、删除线、行内代码和链接
- 引用块，并在左侧显示竖线
- Speaker Notes
- Markdown 表格
- 本地图片
- fenced code block
- 可编辑的仿 SmartArt 图形，由 PowerPoint 普通 shapes 生成

不建议使用 HTML、Mermaid、脚注、LaTeX 数学公式、任务列表等复杂扩展语法。

---

## 3. 幻灯片基本结构

### 3.1 幻灯片分隔符：`---`

建议每张幻灯片之间使用单独一行 `---` 分隔。

```markdown
## 第一页标题

- 第一页内容。

---

## 第二页标题

- 第二页内容。
```

- `---` 单独占一行时表示新幻灯片。
- 如果未使用 `---`，转换器也会尽量按 `##` 标题拆分幻灯片。
- 为保证稳定，推荐显式使用 `---`。

### 3.2 封面页：`#`

如需封面页，在文件最开头用 `#` 写标题，只出现一次：

```markdown
# 演示文稿主标题
演讲人姓名 | 年份
```

- `#` 标题渲染为封面页主标题。
- 紧跟在 `#` 行之后可写一行 `演讲人 | 年份` 格式的副行。
- 副行建议控制在 20 个字符以内，避免折行。
- 如不需要封面，直接从 `##` 开始。

### 3.3 内容页标题：`##`

每张内容幻灯片以 `##` 标题开始。

```markdown
## 幻灯片标题
```

- 标题应简洁，建议控制在 30 个中文字符或 8 个英文单词以内。
- `## Slide 1｜标题`、`## Slide 1 - 标题` 等写法会自动提取后半部分作为标题。

### 3.4 页内小标题：`###` 至 `######`

```markdown
### 小节标题
```

- 建议优先使用 `###`。
- 小节标题建议控制在 20 个中文字符或 5 个英文单词以内。
- 更深层级标题会以较小样式显示，不建议过多使用。

---

## 4. 正文内容语法

### 4.1 普通段落

可以直接写普通文本段落。

```markdown
本页用于概括项目目标和整体策略，重点说明本项目并非单一诉讼。
```

- 普通段落适合背景说明、过渡说明或结论概括。
- 不宜在一页放置过长段落，建议每段控制在 80 个中文字符或 35 个英文单词以内。

### 4.2 无序列表

支持 `-`、`*`、`+` 三种无序列表，推荐统一使用 `-`。

```markdown
- 第一项内容。
- 第二项内容。
  - 二级内容。
```

- 每条要点建议控制在 60 个中文字符或 25 个英文单词以内。
- 支持基本缩进层级，但为保证 PPT 可读性，不建议超过两级。

### 4.3 有序列表

支持 `1.`、`2.` 或 `1)`、`2)` 等有序列表。

```markdown
1. 完成诉前评估。
2. 固定被诉产品样品。
3. 启动诉讼和行政程序。
```

- 有序列表适合步骤、阶段、流程。
- 转换器会保留编号文本。

### 4.4 引用块

支持 `>` 引用块，渲染为左侧竖线 + 缩进文字。

```markdown
> 专利诉讼中，买到产品并不难；难的是让法院相信送检产品的同一性。
> 连续多行引用会合并为同一个引用块。
```

- 适合放置结论句、法院观点、警示语或需要视觉强调的关键判断。
- 连续多行 `>` 会合并为一个引用块，共用左侧竖线。
- 如需多个独立引用块，请在两个引用块之间留一个空行。
- 建议每页不超过 2 个引用块。

### 4.5 分隔线

```markdown
---
```

- 当 `---` 位于两个幻灯片之间时，作为分页符。
- 当页面内容内部使用时，可作为视觉分隔线，但不建议频繁使用。

### 4.6 代码块

支持 fenced code block：

````markdown
```python
print("hello")
```
````

- 代码块会以等宽字体显示。
- 代码内容过长时可能溢出，建议拆页或删去无关代码。

### 4.7 行内代码

```markdown
请注意 `##` 表示幻灯片标题。
```

- 行内代码适合标识命令、文件名、参数、函数名或 Markdown 标记。

---

## 5. 表格

支持标准 GitHub Flavored Markdown 管道表格。

```markdown
| 风险类型 | 具体风险 | 应对措施 |
| --- | --- | --- |
| 方法专利举证风险 | 难以直接适用举证责任倒置 | 推动法院要求被告说明制造方法 |
| 参数鉴定风险 | 单方鉴定可能被质疑 | 强化检材来源和检测方法 |
```

- 表格必须包含表头行、分隔行和至少一行数据。
- 分隔行必须使用 `| --- | --- |` 或同类标准写法。
- 建议每张表不超过 5 列、8 行。
- 单元格尽量控制在 40 个中文字符或 20 个英文单词以内。
- 复杂表格应拆成多页。
- 表格页通常不宜再放置大量正文或大型 SmartArt。

---

## 6. 图片

支持本地图片语法。

```markdown
![图片说明](images/example.png)
```

- 支持相对于 Markdown 文件所在目录的本地图片路径。
- 如图片不存在或为远程 URL，转换器会显示图片占位提示。
- 建议每页最多 1–2 张图片。
- 图片说明会作为占位或辅助文本使用。

---

## 7. SmartArt 图形（仿 SmartArt）

转换器支持多种仿 SmartArt 图形，全部由普通 PowerPoint shapes 生成，可在 PowerPoint 中编辑。SmartArt 会按照 Markdown 中出现的位置排版；如果前面有普通段落、表格或代码块，图形会自动下移。

### 7.1 基本语法

```markdown
:::smartart type="<类型>" style="<样式>" theme="<颜色主题>" size="<尺寸>"
- 项目一
- 项目二
  - 子项目（仅 hierarchy 支持嵌套）
:::
```

- `:::smartart` 和 `:::` 必须各自单独占一行。
- 属性值用双引号包裹。
- 列表项用标准 `-` 写法。
- 每页建议只使用一个 SmartArt；如需多个图形，建议拆分为多页。
- 节点文字会左右、上下居中显示。
- 层级图中的连接线会绘制在节点框下方，节点框和文字会显示在线条之上。

### 7.2 SmartArt 类型

| type | 适用场景 | style 可选值 | 特殊语法 |
| --- | --- | --- | --- |
| `process` | 阶段、步骤、流程 | `chevron`、`numbered` | — |
| `cycle` | 闭环、持续监控、循环管理 | — | — |
| `hierarchy` | 团队分工、组织架构、树形关系 | — | 用两个空格缩进表示子节点 |
| `matrix` | 风险高低、价值高低、优先级分类 | — | `size="2x2"` 或 `size="2x3"` |
| `pyramid` | 优先级、层级递进、价值分层 | — | 第一项在顶部 |
| `timeline` | 时间安排、项目节点、诉讼进度 | — | 用 `标签 | 说明` 分隔时间点与描述 |

### 7.3 SmartArt 选择建议

| 场景 | 推荐 type |
| --- | --- |
| 阶段、步骤、流程 | `process` |
| 闭环、持续监控、循环管理 | `cycle` |
| 团队分工、组织架构、树形关系 | `hierarchy` |
| 风险高低、价值高低、优先级分类 | `matrix` |
| 优先级、层级递进、价值分层 | `pyramid` |
| 时间安排、项目节点、诉讼进度 | `timeline` |

### 7.4 颜色主题

支持：`blue`（默认）、`teal`、`green`、`orange`、`purple`、`mono`。

### 7.5 hierarchy 缩进规则

`hierarchy` 中每一级缩进使用两个空格，不建议混用 Tab 和空格。

```markdown
:::smartart type="hierarchy" theme="blue"
- 专利维权项目组
  - 诉讼组
    - 诉状起草
    - 庭审准备
  - 无效组
    - 现有技术检索
    - 无效请求撰写
:::
```

### 7.6 timeline 写法

```markdown
:::smartart type="timeline" theme="blue"
- Week 1 | Evidence Preservation
- Week 3 | Complaint Filed
- Month 2 | Invalidity Request
:::
```

- `|` 两侧建议各留一个空格。
- 左侧为标签或时间点，右侧为说明。

---

## 8. 行内格式

### 8.1 粗体

```markdown
这是 **重点内容**。
```

### 8.2 斜体

```markdown
这是 *强调说明*。
```

### 8.3 粗体斜体

```markdown
这是 ***特别强调***。
```

### 8.4 删除线

```markdown
这是 ~~已删除或已修改的内容~~。
```

删除线表示原本有此规定但已被删除或修改，通常具有实质意义，必须保留。

### 8.5 链接

```markdown
请参见 [CNIPA 官网](https://www.cnipa.gov.cn)。
```

- 转换器会尽量保留为 PowerPoint 中的可点击链接。
- 为避免版面过长，建议正文中显示链接文本，不直接显示长 URL。
- 如 URL 本身必须展示，可另起一条短要点写出。

---

## 9. Speaker Notes

支持以下写法添加讲者备注：

```markdown
> **Speaker note:** 这里写演讲者自己看的提示。
> 可继续写第二行备注。
```

- Speaker Note 不会显示在幻灯片正文上。
- 转换器会将其写入 PowerPoint 的笔记栏。
- 每页最多一个 Speaker Note 块，建议放在页面内容末尾、`---` 之前。

---

## 10. 每页容量建议

### 10.1 中文 / 日文内容

| 项目 | 建议值 | 上限 |
| --- | --- | --- |
| 每页小标题数 | 2–3 个 | 4 个 |
| 每页要点总数 | 5–8 条 | 10 条 |
| 每条要点字数 | 30–50 字 | 60 字 |
| 表格列数 | 2–4 列 | 5 列 |
| 表格行数 | 3–6 行 | 8 行 |

### 10.2 英文内容

| 项目 | 建议值 | 上限 |
| --- | --- | --- |
| 每页小标题数 | 2–3 个 | 3 个 |
| 每页要点总数 | 4–6 条 | 8 条 |
| 每条要点词数 | 15–25 词 | 35 词 |
| 表格列数 | 2–4 列 | 5 列 |
| 表格行数 | 3–5 行 | 7 行 |

### 10.3 通用原则

- 内容太多时，应拆分为多张幻灯片，宁可多一页。
- 表格页应减少正文要点，代码页、图片页和 SmartArt 页也应减少其他文字。
- 混合语言按中文标准估算，但长英文句子更容易换行。
- 不要用大量空行、HTML 或手工缩进来模拟版面。

---

## 11. 内容完整性要求（最高优先级）

分页和格式调整的唯一目的是适配容量，绝不能以此为由删减原始内容。

### 11.1 必须逐字或实质完整保留的内容

1. **所有数字和统计数据**
   - 百分比、时间、金额、倍数、页码、案号、专利号、申请号、日期等。
   - 数字是具体证据，删掉等于破坏论点。

2. **所有专有名词、产品名、主体名和案例名**
   - 不得用泛称替代专有名词。

3. **删除线内容 `~~text~~`**
   - 删除线表示历史变化或被删除内容，必须保留。

4. **引用块的完整文字 `>`**
   - 引用块通常是结论性语句，不得截短。

5. **封面标题**
   - `#` 标题必须与原文完全相同，不得缩写。

6. **关键法律、技术或商务术语**
   - 原文使用的专业术语不得随意替换为同义词。

### 11.2 允许与不允许

| 允许 | 不允许 |
| --- | --- |
| 把一页内容拆为两页 | 删掉任何项目符号 |
| 把 `###` 标题改为页标题 `##` | 把两个实质不同的要点合并为一个 |
| 在新页重复必要背景信息 | 用“等”替代列举中的项目 |
| 在不改变论证逻辑的前提下微调相邻内容顺序 | 改写或简化关键措辞 |
| 把长段落拆成多条要点 | 删除数字、主体名称或限制条件 |

如原材料已有明确顺序，应优先保留原顺序。

### 11.3 自查清单

输出前请确认：

- [ ] 原文的每一个数字是否都出现在输出中？
- [ ] 原文的每一条项目符号是否都保留了？
- [ ] 原文的每一个引用块是否完整保留？
- [ ] 原文的删除线内容是否保留？
- [ ] 原文的封面标题是否与原文完全一致？
- [ ] 原文提到的每个专有名词是否都出现在输出中？

---

## 12. 不建议使用的内容

不要使用下列内容控制格式：

- HTML 标签，例如 `<br>`、`<span>`、`<div>`。
- 脚注，例如 `[^1]`。
- Mermaid 图，建议改用 `:::smartart` 或图片。
- LaTeX 数学公式，建议改为普通文本或图片。
- 过深嵌套列表。
- 超大表格。
- 大量空行或手工缩进。
- 远程图片 URL。

---

## 13. 完整示例

```markdown
# 专利维权项目工作方案
张律师 | 2026

---

## 项目目标与整体策略

本项目并非单一诉讼，而是一组围绕产品 X 展开的系列维权行动。

### 三条主线

- 第一，围绕三件发明专利分别设计侵权比对和证据路径。
- 第二，提前收集销售额、利润率和销售持续时间等赔偿证据。
- 第三，通过保全、鉴定和行政程序提高执行压力。

> **Speaker note:** 本页重点说明项目不是单案诉讼，而是整体证据工程。

---

## 项目流程

:::smartart type="process" style="chevron" theme="blue"
- 证据收集
- 侵权比对
- 提起诉讼
- 庭审听证
- 裁决结案
:::

---

## 项目风险矩阵

| 风险类型 | 具体风险 | 应对措施 |
| --- | --- | --- |
| 方法专利举证风险 | 难以直接适用举证责任倒置 | 通过初步举证推动被告说明制造方法 |
| 参数鉴定风险 | 单方鉴定可能被质疑 | 强化检材来源、封存过程和检测方法 |
| 赔偿证据风险 | 销售额和利润率证据不足 | 诉前收集销售规模和行业利润率证据 |
```

---

## 14. 输出要求

- 只输出 Markdown 文本，不要附加解释、说明或前言。
- 不要输出文件名或任何非 Markdown 内容。
- 第一张幻灯片直接从标题开始。
- 建议使用 `---` 明确分隔每张幻灯片。
- 输出前必须完成“11. 内容完整性要求”中的自查清单。

---

现在请处理我提供的以下材料：

[在此粘贴你的材料]


> 注意：项目模板的最后一页会自动作为结束页/感谢页追加。除非用户特别要求，不要在 Markdown 正文中额外生成 Thank you / 感谢页。


## SmartArt 同页多行排列提示

转换器会对过长 SmartArt 进行同页多行排列：

- `process` 超过 5 个顶层节点时在同一页换行；
- `timeline` 超过 6 个时间节点时在同一页换行；
- `cycle` 超过 6 个节点时在同一页换行；
- `pyramid` 超过 5 层时在同一页换行；
- `matrix` 与 `hierarchy` 默认不自动换行。

即使支持同页多行排列，写作时仍建议尽量控制每页信息量。  
如果内容具有明确逻辑阶段，优先由作者主动拆成多页。

补充：同页多行排列会避免最后一行只有 1 个节点，例如 6 个节点排为 4+2，11 个节点排为 5+4+2。
